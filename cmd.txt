git init // Initialize a repository
git add <file or folder> // Moving files to the staging area
git commit <"m">// Moving files and folders to the staging area.
git add . // Add all files and folders.
git log // Enables us to see our commit History

git checkout // Enables us to checkout a commit or branch-switching.

git branch // View all branch in your code.

// IF you want to creat an new branch

git branch <name> // Creating a new Branch.



git configure git.

<Enables us to know who did what.>
<Enable us to push our code to the cloud>
<Enables to use the editor of your choice>


configure global configuration. 
configure localy.

local configuration come before global.

user Name and User email.

git config --global user.name <user_name>
git config --global user.email <user_email>

to check you configuration

cmd: git config --list

config global editor
-- git config --global core.editor "code --wait"